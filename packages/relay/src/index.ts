/** SES-RDP relay entry. Phase 0 placeholder; Phase 1 adds the /ws device hub. */
import { SES_RDP_PROTOCOL_VERSION } from '@ses-systems/rdp-shared';
export const RELAY_PROTOCOL_VERSION = SES_RDP_PROTOCOL_VERSION;
